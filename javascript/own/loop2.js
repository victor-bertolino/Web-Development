const nome = "Victor Bertolino";

for (let i = 0; i < nome.length; i++) {
    const letra = nome[i];
    if (letra === "a" || letra === "e" || letra === "i" || letra === "o" || letra === "u") {
console.log(letra);
    };
};